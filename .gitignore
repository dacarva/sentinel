# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo
tmp/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*

# Test & coverage
coverage/
.nyc_output/
.vitest/

# IDE & OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Misc
*.tgz
.cache/
mock-bank/certs/

# Backend runtime data (LOCAL DEVELOPMENT ONLY)
# ⚠️  For production, use PostgreSQL or other persistent storage
# See: docs/PRODUCTION_DEPLOYMENT.md
backend/data/
data/attestations/

# Built TLSNotary plugins (generate with: bun run plugin:build)
app/public/mock-bank-plugin.wasm
app/public/ts-plugin-*.js

# Noir circuit compiled artifacts — generated in CI/CD, not committed to repo.
# Run: cd circuit && nargo compile && bb write_vk -b target/balance_threshold.json -o target/
# See: docs/ZK_CIRCUITS.md
circuit/target/

# Circuit artifact served to the browser (copied from circuit/target/ during build)
# app/public/circuit.json

# Binaryen (downloaded by extism-js install for WASM compile; not project source)
binaryen/
binaryen*

# Database & migrations (SHOULD be committed for production)
!database/
!database/schema.sql
!database/migrations/

# Test certificates & keys (SHOULD be committed for local dev)
!mock-bank/certs/generate.sh
!mock-bank/certs/notary-key.ts
