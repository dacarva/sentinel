{
  "name": "zk-credit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["mock-bank","app","backend","plugin-sdk", "tlsn-common", "plugin"],
  "scripts": {
    "bank:start": "bun run --filter mock-bank start",
    "app:dev": "bun run --filter app dev",
    "app:build": "bun run --filter app build",
    "app:lint": "bun run --filter app lint",
    "app:preview": "bun run --filter app preview",
    "sentinel:start": "bun run --filter backend start",
    "plugin:build": "bun run --filter @tlsn/ts-plugin-sample build && cp plugin/build/ts-plugin-sample.js app/public/ts-plugin-sample.js",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
