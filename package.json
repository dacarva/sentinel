{
  "name": "zk-credit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["mock-bank"],
  "scripts": {
    "bank:start": "bun run --filter mock-bank start",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
