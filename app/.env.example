# Sentinel backend API base URL (for POST /attest)
VITE_SENTINEL_API=http://localhost:3000

# TLSNotary plugin WASM URL (Mock Bank plugin when built)
# Default: /mock-bank-plugin.wasm (served from app public folder)
# VITE_TLSN_PLUGIN_URL=/mock-bank-plugin.wasm
